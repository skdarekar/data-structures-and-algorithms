// Detect loop in linkedList
class Node {
    constructor(element) {
        this.element = element;
        this.next = null;
    }
}

class LinkedList {
    constructor(head = null) {
        this.head = head;
    }

    addElement(element) {
        // todo
    }

    printList() {
        // todo
    }

    detectLoop() {
        // todo
    }
}

let list = new LinkedList();