// Implement linked list
class Node {
    constructor(element){
        this.element = element;
        this.next = null;
    }
    // functions to be implemented
    // addElement
    // insertAt(element, location)
    // removeFrom(location)
    // removeElement(element)

    // Helper methods
    // isEmpty
    // size_Of_list
    // PrintList
}

let firstNode = new Node(10);
console.log(firstNode.element)